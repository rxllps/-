# TODO Лист - Django приложение

Простое веб-приложение для управления задачами на Django с полной CRUD-функциональностью.

## Функции

- Создание новых задач
- Просмотр списка всех задач
- Редактирование существующих задач
- Удаление задач
- Отметить задачу как выполненную
- Красивый и удобный интерфейс

## Запуск проекта

### 1. Установите зависимости

```bash
uv sync
```

### 2. Активируйте окружение

```bash
source .venv/bin/activate
```

### 3. Примените миграции

```bash
python manage.py migrate
```

### 4. Запустите сервер

```bash
python manage.py runserver
```

Откройте браузер и перейдите на `http://localhost:8000`

### 5. Создайте суперпользователя (опционально для админ-панели)

```bash
python manage.py createsuperuser
```

### 6. Запустите сервер

```bash
python manage.py runserver
```

Приложение будет доступно по адресу: `http://127.0.0.1:8000/`

## Использование

### Главная страница
- Просмотр всех задач
- Добавление новой задачи (кнопка "+ Добавить новую задачу")

### Редактирование задачи
- Нажмите кнопку "Редактировать" рядом с задачей
- Измените необходимые поля
- Сохраните изменения

### Удаление задачи
- Нажмите кнопку "Удалить" рядом с задачей
- Подтвердите удаление

### Админ-панель
- Перейдите на `http://127.0.0.1:8000/admin/`
- Входите с учетными данными суперпользователя
- Управляйте задачами через админ-интерфейс

## Модель Task

```python
class Task(models.Model):
    title = models.CharField(max_length=200)           # Название задачи
    description = models.TextField(blank=True)         # Описание задачи
    created_at = models.DateTimeField(auto_now_add=True)  # Дата создания
    completed = models.BooleanField(default=False)    # Статус выполнения
```

## Структура проекта

```
.
├── todoproject/          # Основные настройки проекта
│   ├── settings.py      # Настройки Django
│   ├── urls.py          # Маршруты приложения
│   └── ...
├── tasks/               # Приложение управления задачами
│   ├── models.py        # Модель Task
│   ├── views.py         # Представления (CRUD операции)
│   ├── forms.py         # Django Forms
│   ├── admin.py         # Админ-интерфейс
│   ├── urls.py          # Маршруты приложения
│   └── migrations/      # Миграции БД
├── templates/tasks/     # HTML шаблоны
│   ├── base.html        # Базовый шаблон
│   ├── task_list.html   # Список задач
│   ├── task_form.html   # Форма создания/редактирования
│   └── task_confirm_delete.html  # Подтверждение удаления
├── db.sqlite3           # SQLite база данных
├── manage.py            # Django утилита управления
├── pyproject.toml       # Конфиг UV
└── README.md            # Этот файл
```

## Зависимости

Все зависимости указаны в `pyproject.toml`:

```toml
[project]
name = "todo-app"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "django>=5.2.9",
]
```

## Технологии

- **Django 5.2.9** - веб-фреймворк
- **SQLite** - база данных
- **HTML/CSS** - фронтенд
- **UV** - менеджер пакетов Python

## Лицензия

MIT

